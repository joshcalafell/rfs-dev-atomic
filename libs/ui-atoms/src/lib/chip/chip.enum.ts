export enum EChipThemeColor {
	primary = 'primary',
	secondary = 'secondary',
	success = 'success',
	warning = 'warning',
	error = 'error',
	info = 'info',
	disabled = 'disabled',
}

export enum EChipIconType {
	add = 'add',
	remove = 'remove',
	check = 'check',
	done = 'done',
	clear = 'clear',
}

export enum EIconPosition {
	left = 'left',
	right = 'right',
}

export enum EIconColor {
	primary = 'primary',
	secondary = 'secondary',
	success = 'success',
	warning = 'warning',
	error = 'error',
	info = 'info',
	disabled = 'disabled',
}

export enum EChipSize {
	small = 'small',
	medium = 'medium',
	large = 'large',
}

export enum EIconTooltipPosition {
	top = 'top',
	bottom = 'bottom',
	left = 'left',
	right = 'right',
}
